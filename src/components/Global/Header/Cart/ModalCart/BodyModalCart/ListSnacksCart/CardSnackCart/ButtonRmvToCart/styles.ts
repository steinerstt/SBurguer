import styled from "styled-components";

export const ContainerButtonRmvToCart = styled.button`
  position: absolute;
  top: 0.25rem;
  right: 0.25rem;
  padding: 0.125rem;

  svg {
    font-size: 1.5625rem;
    color: var(--color-gray-200);
  }
`;
